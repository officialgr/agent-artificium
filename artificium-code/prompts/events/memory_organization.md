[SYSTEM GUIDANCE NOTIFICATION — MEMORY ORGANIZATION]

A semantic memory was {{action}}.

Memory path: {{memory_path}}
Suggested parent index: {{parent_index_path}}
Meta-memory: {{meta_memory_path}}
Retrieval purpose: {{retrieve_when}}

The harness deliberately did not generate or edit an index or meta-memory entry.
Organization is part of your cognition.

Think about the memory's meaning and relationship to what already exists. Then,
when useful:

- Create or update the nearest meaningful folder's `index.txt` with what the
  folder contains, how its memories relate, which are important, and when to
  retrieve them.
- Update higher indexes only as needed to preserve a clear route from
  meta-memory to this memory.
- Update `meta_memory.md` when this creates or changes an important branch,
  exceptional direct memory, always-needed fact, harness guide, or major tool.
- Merge, move, rename, or revise memories if the new file exposed a better
  organization. Repair all affected paths in the same task.

Do not add every file directly to meta-memory and do not create decorative index
entries without retrieval value. Before continuing, ensure the memory remains
findable from the paths that a future instance will actually inspect.

Normal memories have no size target. If this memory becomes unwieldy, split and
index it according to meaning; never discard unique useful information merely
for brevity.
