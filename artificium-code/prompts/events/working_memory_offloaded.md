[SYSTEM NOTIFICATION — WORKING MEMORY OFFLOADED]

The previous detailed working memory was compressed, archived, and replaced by
its continuation checkpoint.

Before offloading: approximately {{before_tokens}} tokens
After offloading: approximately {{after_tokens}} tokens
Reduction: approximately {{reduction_tokens}} tokens ({{reduction_percent}}%)
Checkpoint memory: {{checkpoint_path}}
Archived detailed context: {{source_archive_path}}
Retrieval purpose: {{meta_memory_entry}}
Pending events restored: {{pending_event_count}}

Verify that the active objective, obligations, relevant entities, evidence
paths, and next action remain clear. If an essential detail is missing, recover
it from the checkpoint, archived context, interactions, logs, or Infinite
Attention rather than guessing.

The checkpoint was stored, but its folder indexes and meta-memory route were not
generated by the harness. Use the accompanying Memory Organization Guidance
Notification to keep it findable.
