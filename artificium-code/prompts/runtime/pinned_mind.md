[PINNED SELF — freshly loaded from `mind/self.txt`]

{{self_content}}

[END PINNED SELF]

[PINNED META-MEMORY — freshly loaded from `mind/meta_memory.md`]

{{meta_memory_content}}

[END PINNED META-MEMORY]

Self and meta-memory are reloaded from disk for every inference. They are not
stale copies recovered from a prior working-memory-offload checkpoint. The
complete meta-memory is loaded without an artificial harness truncation limit;
Guidance Notifications ask you to reduce it when its recurring context cost
becomes excessive.
