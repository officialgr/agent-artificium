[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "artificium-agent"
version = "1.9.3"
description = "A continual-learning agent with unified memory and Infinite Attention"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
artificium = "artificium.cli:main"

[tool.setuptools]
packages = ["artificium"]
